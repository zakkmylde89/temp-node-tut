// Set Interval Example

setInterval(() => {
    console.log('hello world');
}, 2000);
console.log('I will run first');
// process stays alive unless
// Kill Process CONTROL + C
// unexpected error

// setInterval runs in intervals
// so this means it will continuously run at the set interval
// declared in calling it